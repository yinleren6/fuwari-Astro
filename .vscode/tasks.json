{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "pnpm biome format --write . && pnpm run build",
			"type": "shell",
			"command": "pnpm biome format --write . && pnpm run build",
			"args": [],
			"options": {
				"cwd": "${workspaceFolder}",
				"envFile": "${workspaceFolder}/.env"
			},
			"group": {
				"kind": "build",
				"isDefault": false
			},
			"problemMatcher": ["$tsc"],
			"presentation": {
				"reveal": "always",
				"panel": "new"
			}
		}
	]
}
